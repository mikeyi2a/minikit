"use client";

// Stub — ProgressBar implementation pending.
import * as React from "react";

export interface ProgressBarProps {
  label?: string;
  value: number;
  variant?: "determinate" | "indeterminate";
  showValue?: boolean;
  className?: string;
}

export function ProgressBar(_props: ProgressBarProps): React.ReactElement | null {
  return null;
}
