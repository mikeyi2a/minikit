"use client";

// Stub — CanvasFrame implementation pending.
import * as React from "react";

export interface CanvasFrameProps {
  children?: React.ReactNode;
  className?: string;
  width?: number;
  height?: number;
}

export function CanvasFrame(_props: CanvasFrameProps): React.ReactElement | null {
  return null;
}
